# Server configuration
PORT=4000
# Allowed CORS origin(s), comma-separated or *
ORIGIN=*
# Optional: default room to join when none provided
DEFAULT_ROOM=

# Spotify PKCE configuration
# Required for /spotify/exchange and /spotify/refresh
SPOTIFY_CLIENT_ID=
SPOTIFY_REDIRECT_URI_WEB=http://localhost:5173/callback

# No third-party secrets in repo; add your own locally if needed.
